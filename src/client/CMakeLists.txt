# file(GLOB CLIENT_SOURCES "*.cpp")
# file(GLOB CLIENT_HEADERS "${CMAKE_SOURCE_DIR}/include/client/*.h")
#
# add_library(ClientLib ${CLIENT_SOURCES} ${CLIENT_HEADERS})
#
# target_include_directories(ClientLib PUBLIC ${CMAKE_SOURCE_DIR}/include)

